# Managed by Ansible (personal-infra, role: postgres) — edits will be reverted.
# Mode 0600, root-owned. This is the superuser password; per-project passwords
# are written into each project's own .env by the `deploy` role.
POSTGRES_PASSWORD={{ vault_postgres_superuser_password }}
